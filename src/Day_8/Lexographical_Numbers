  public void help(int curr , int tar , List<Integer> ans){
        if(curr > tar)return;

        ans.add(curr);
        for(int i = 0;i <= 9;i++){
            help(curr * 10 + i , tar , ans);
        }
        return;
    }
    public List<Integer> lexicalOrder(int n) {
        List<Integer> ans = new ArrayList<>();
        for(int i = 1; i <= 9;i++){
            help(i , n , ans);
        }
        return ans;
    }